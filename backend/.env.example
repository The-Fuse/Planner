# PostgreSQL connection string (optional — falls back to SQLite)
DATABASE_URL=

# ntfy.sh topic for daily notifications (set via the app UI or directly here)
# The topic is also stored in the database via POST /api/preferences key=ntfy_topic
NTFY_TOPIC=

# Frontend URL — tapping the notification opens this URL (launches the installed PWA)
APP_URL=https://planner-936q.onrender.com
